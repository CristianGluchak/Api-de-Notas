spring:
  datasource:
    url: jdbc:mysql://localhost:3306/apinotas?useSSl=false&createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update